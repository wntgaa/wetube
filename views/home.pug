extends layouts/main
include mixins/videoBlock

block content
    .videos 
        each item in videoin
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            }) 